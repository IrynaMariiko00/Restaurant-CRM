# URL бекенду для Vite dev proxy (vite.config.ts) і Vercel rewrites (vercel.json).
# НЕ використовується в браузерних запитах — лише для проксі на сервері.
VITE_API_URL=https://accanto.adammudrak.pp.ua

# Development — залиш порожнім, використовується proxy з vite.config.ts
