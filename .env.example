# Залиш порожнім — запити йдуть на той самий домен (/auth/*) і проксуються на бекенд
# (Vite proxy у dev, Vercel rewrites у prod). Прямий URL бекенду тут викличе CORS у браузері.
VITE_API_URL=

# Development — залиш порожнім, використовується proxy з vite.config.ts
